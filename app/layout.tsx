import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Flow - Habit Formation Through Identity",
  description: "Stop tracking habits. Start becoming the person who naturally does them. Flow creates the environment for effortless habit formation.",
  authors: [{ name: 'Flow App' }],
  keywords: "habits, identity, behavioral psychology, habit formation, Zeigarnik effect, focus, meditation"
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
